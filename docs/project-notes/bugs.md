# Bug Log

Recurring bugs and their solutions. Search here before debugging a familiar-looking issue.

| Date | Summary | Root Cause | Fix | Ticket |
|------|---------|------------|-----|--------|
| 2026-04-16 | `main.py` is a plain script, not a FastAPI app | Generated by `uv init` default | Replace with FastAPI app factory | #3 |
| 2026-04-16 | `models/base.py` uses stdlib `dataclass` + `ABC` instead of SQLAlchemy `DeclarativeBase` | Incorrect base class | Rewrite with SQLAlchemy declarative base, UUID PK, `created_at`/`updated_at` | #7 |
| 2026-04-16 | `models/user.py` has wrong fields (`name` instead of `hashed_password`, dataclass inheritance) | Built on wrong base | Rewrite as SQLAlchemy model matching domain spec | #7 |
| 2026-04-16 | Import path `from src.app.models.base` won't resolve at runtime | Should be `from app.models.base` (src layout uses `app` as the package root) | Fix imports to use `app.` prefix | #7 |
